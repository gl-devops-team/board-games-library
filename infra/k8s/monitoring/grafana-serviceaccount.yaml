apiVersion: v1
kind: ServiceAccount
metadata:
  name: grafana
  namespace: monitoring
  annotations:
    # Replace with: terraform -chdir=infra/terraform/environments/dev output -raw grafana_irsa_role_arn
    eks.amazonaws.com/role-arn: REPLACE_WITH_TERRAFORM_OUTPUT_grafana_irsa_role_arn